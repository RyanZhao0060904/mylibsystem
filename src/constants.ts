// ============ 全局业务常量 ============
// 把「图书分类 / 图书状态 / 借阅状态」等离散值统一收敛到这里，好处是：
//   1) 多处（表单下拉、表格渲染、筛选）共用同一份数据源，改一处全项目生效，避免魔法字符串散落各处；
//   2) 用 as const 声明为「只读字面量类型」，TypeScript 可推导出精确的联合类型，
//      在类型层面就杜绝了拼写错误（例如把分类写成 '历史学' 会直接报错）。
// 全局业务常量

/** 图书分类 */
export const BOOK_CATEGORIES = ['计算机', '文学', '历史', '科技', '艺术'] as const

/** 图书状态 */
export const BOOK_STATUSES = ['在库', '借出'] as const

/** 借阅状态（含逾期） */
export const BORROW_STATUSES = ['借阅中', '已归还', '已逾期'] as const
